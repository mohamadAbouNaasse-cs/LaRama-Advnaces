/**
 * Administrative Authentication Context Definition - LaRama Frontend
 * Supplies a React context instance for admin authentication state.
 */

import { createContext } from 'react';

export const AdminAuthContext = createContext(null);
